kind = "web"
previewPath = "/"
title = "ISP Management System"
version = "1.0.0"
id = "artifacts/isp-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19580

[services.development]
run = "pnpm --filter @workspace/isp-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/isp-portal", "run", "build" ]
publicDir = "artifacts/isp-portal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19580"
BASE_PATH = "/"
